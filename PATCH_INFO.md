# wallpaper_app 壁纸铺满方式修复补丁（二版）

本补丁基于 `wallpaper_app` 的 `main` 分支，包含前一版六种 Windows 壁纸模式修复，以及本版 Fit 黑边修复；未推送远程仓库。

## 本版新增修复

选择「适应」时，先将图片目标尺寸取整，再将剩余画布像素从两侧对称分配。上下黑边、左右黑边会分别保持对称；遇到奇数像素时，两侧最多只相差 1 像素，不会出现一侧有明显黑边而另一侧没有。

## 文件路径

补丁内保留从仓库根目录开始的完整相对路径，可直接覆盖对应文件。

## 验证

已执行 `git diff --check`，并确认 ZIP 压缩包完整性。
