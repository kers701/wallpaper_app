# wallpaper_app 等待归零与过期自动更换修复补丁

本补丁基于 `wallpaper_app` 的 `main` 分支，包含当前工作区此前壁纸铺满方式修复，以及本次等待时间归零/过期调度修复；未推送远程仓库。

## 本次修复

「等待时间归零」不再把上次更换时间写成过去时间后等待服务碰运气，而是把下一次更换安排为当前时间约一分钟后，并立即唤起前台自动更换服务。

前台服务使用明确的 `dueAt` 判断当前时间是否已经晚于下次更换时间。归零后到期、普通周期到期和首次自动更换均走同一套正常 `changeOnce` 流程，包括下载/预下载、桌面与锁屏写入、历史记录、缓存、统计、时间戳和通知刷新。成功后同时更新跨进程时钟与 DataStore，避免界面继续显示旧的下次更换时间。

## 文件路径

补丁内保留从仓库根目录开始的完整相对路径，可直接覆盖对应文件。

## 验证

已执行 `git diff --check`，检查了归零一分钟顺延、到期判断、正常更换入口和时间同步代码，并通过 ZIP 完整性测试。
