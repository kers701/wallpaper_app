# wallpaper_app 壁纸铺满方式修复补丁

本补丁基于仓库 `wallpaper_app` 的 `main` 分支生成，未推送远程仓库。

## 修改内容

- 对齐 Windows 壁纸模式：填充、适应、拉伸、平铺、居中、跨区。
- 新增 `Tile` 与 `Span` 模式及持久化代码值。
- 修正实际 Bitmap 绘制逻辑：Fill 等比覆盖裁边，Fit 等比完整显示，Stretch 非等比铺满，Tile 原图像素重复，Center 原图像素居中，Span 在整个系统壁纸画布上连续显示。
- 更新设置页说明。

## 文件路径

补丁内文件均保留从仓库根目录开始的完整相对路径，可直接覆盖对应文件。

## 验证说明

已执行 `git diff --check`。Android 构建未能在当前沙盒完成，因为环境未安装 Android SDK；代码引用检查已确认六种模式均已接入实际渲染分支。
