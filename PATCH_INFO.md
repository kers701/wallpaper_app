# wallpaper_app 壁纸铺满方式修复补丁（三版）

本补丁基于 `wallpaper_app` 的 `main` 分支，包含前两版六种 Windows 壁纸模式与 Fit 黑边修复，以及本版 Fill 修复；未推送远程仓库。

## 本版修复

普通壁纸模式现在以实际屏幕尺寸作为变换目标，不再默认使用可能是屏幕两倍大小的 Android `desiredMinimumWidth/Height` 虚拟壁纸画布，避免 Fill 结果被无谓放大后再次裁切。

Fill 始终按等比覆盖目标画布计算比例：原图小于屏幕时等比放大，原图大于屏幕时等比缩小，最后仅裁掉覆盖后多余的边缘；图片不会因为尺寸较大而直接以原始尺寸放大显示。

Span 仍使用系统提供的跨区壁纸画布。

## 文件路径

补丁内保留从仓库根目录开始的完整相对路径，可直接覆盖对应文件。

## 验证

已执行 `git diff --check`，确认 ZIP 压缩包完整性，并检查 Fill/Span 目标画布分支。
